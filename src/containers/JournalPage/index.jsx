import { connect } from 'react-redux';

import Page from '../../components/JournalPage';

const mapStateToProps = () => (
  {
  }
);

const mapDispatchToProps = () => (
  {
  }
);

const JournalPage = connect(
  mapStateToProps,
  mapDispatchToProps,
)(Page);

export default JournalPage;
